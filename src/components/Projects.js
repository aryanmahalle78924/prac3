import React from "react";

const Projects = () => {
  return (
    <div
      style={{
        fontSize: "50px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      This is Projects Page
    </div>
  );
};

export default Projects;
