import React from "react";

const About = () => {
  return (
    <div
      style={{
        fontSize: "50px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      This is About Page
    </div>
  );
};

export default About;
