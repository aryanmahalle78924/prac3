import React from "react";

const Home = () => {
  return (
    <div
      style={{
        fontSize: "50px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      This is the HomePage.
    </div>
  );
};

export default Home;
